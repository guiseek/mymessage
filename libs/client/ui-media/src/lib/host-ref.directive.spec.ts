import { HostRefDirective } from './host-ref.directive';

describe('HostRefDirective', () => {
  it('should create an instance', () => {
    const directive = new HostRefDirective();
    expect(directive).toBeTruthy();
  });
});
